# my-quake-shakes
Python based hyperlocal earthquake magnitude processor wrapper. Writes both to SQLite DB and .ics calendar .  Can be used with Home Assistanat to display outcomes.  Uses USGS Earthquake event data and IRIS earthquake sensor data to calculate hyper local magnitude via SAIpy.
